name: tutorial

venv:
  name: mlops
  python: "3.8"
  dependencies:
    - numpy
    - pandas
    - scikit-learn

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 0.5}
      l1_ratio: {type: float, default: 0.1}
    command: "python train.py {alpha} {l1_ratio}"
